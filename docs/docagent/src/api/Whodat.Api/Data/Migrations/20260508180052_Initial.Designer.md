# Initial

> **File:** `src/api/Whodat.Api/Data/Migrations/20260508180052_Initial.Designer.cs`  
> **Kind:** class

```csharp
[DbContext(typeof(WhodatDb))]
    [Migration("20260508180052_Initial")]
    partial class Initial
```


The Initial migration defines the base database schema for the WhodatDb context, primarily configuring ASP.NET Core Identity tables (roles, role claims, user claims, user logins, and user roles). It builds the target model used by EF Core to create the initial identity-related schema when the database is created. Use this migration when setting up a fresh database to establish authentication/authorization support.

## Remarks
- Targets the WhodatDb context and creates Identity-related tables such as AspNetRoles, AspNetRoleClaims, AspNetUserClaims, AspNetUserLogins, and AspNetUserRoles.
- AspNetRoles includes a unique index on NormalizedName named RoleNameIndex, and NormalizedName/Name have a 256-character limit.
- Other identities tables define standard keys and indexes (e.g., RoleId, UserId, and composite keys where applicable).