# HiddenAndAliases

> **File:** `src/api/Whodat.Api/Data/Migrations/20260508184104_HiddenAndAliases.Designer.cs`  
> **Kind:** class

Defines the target model for the 20260508184104_HiddenAndAliases migration. This EF Core migration designer stores the model snapshot after applying the migration so future migrations can compute changes against a stable baseline.

## Remarks

BuildTargetModel uses the Fluent API to configure Identity-related entities and their mappings to the ASP.NET Core Identity tables (AspNetRoles, AspNetRoleClaims, AspNetUserClaims, AspNetUserLogins, AspNetUserRoles, etc.). It sets up keys, indices, column types, and table mappings. The annotation ProductVersion records the EF Core tooling version used to generate the snapshot. The code is auto-generated by EF Core tooling and is intended for migration tooling rather than runtime logic. Modifying it manually can lead to inconsistencies between the model snapshot and the actual runtime model.

> Note: The snippet in this designer shows provider-specific column types (e.g., TEXT, INTEGER), which reflect the underlying database provider (SQLite in this case). The exact types may vary with different providers.

## Example

```csharp
// Example (conceptual): EF Core tooling uses this designer to compare model snapshots
// when generating a new migration. The target model defined here represents the state
// after applying the 20260508184104_HiddenAndAliases migration.
public partial class HiddenAndAliases
{
    protected void BuildTargetModel(ModelBuilder modelBuilder)
    {
        // Identity-related entity configurations and table mappings reside here.
    }
}
```

## Notes

- Auto-generated by EF Core migrations tooling; avoid manual edits unless you fully understand the migration system.
- Represents the model snapshot used for difference calculation in subsequent migrations.
- Includes mappings for Identity-related tables (AspNetRoles, AspNetRoleClaims, AspNetUserClaims, AspNetUserLogins, AspNetUserRoles, etc.) with keys, indices, and provider-specific column types.
- The presence of pragma warning disable indicates suppression of certain obsolete or warning-worthy API usage within generated code.
- The class is decorated with DbContext and Migration attributes and is part of a migration history sequence identified by the timestamp in its name.